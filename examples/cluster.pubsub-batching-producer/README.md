# ClusterPubSubBatchingProducer

Shows how to batch messages when publishing to a Proto.Cluster pub-sub topic. By
grouping messages into batches the producer reduces network overhead and
improves throughput.
