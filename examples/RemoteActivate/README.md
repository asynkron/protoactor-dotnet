# RemoteActivate

Shows how actors can be activated remotely on another node using Proto.Remote.
A process requests the remote system to spawn an actor and then communicates
with it as if it were local.
