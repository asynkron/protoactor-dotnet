# AspNetGrains

Shows how to host Proto.Cluster grains inside an ASP.NET Core application. Two
nodes are run side by side and grains are resolved through HTTP endpoints,
illustrating how web applications can participate in a Proto.Cluster.
