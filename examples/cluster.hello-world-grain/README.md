# ClusterGrainHelloWorld

Minimal example of Proto.Cluster grains where two nodes greet each other with
"Hello World" messages. It demonstrates defining a grain interface, running a
cluster and invoking grain methods across nodes.
