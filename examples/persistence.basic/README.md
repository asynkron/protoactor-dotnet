# Persistence

Shows how to persist actor state using Proto.Actor persistence providers and
replay it on restart. The sample demonstrates event sourcing and snapshots to
recover actors after failure.
