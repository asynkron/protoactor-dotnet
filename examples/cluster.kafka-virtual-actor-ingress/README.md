# KafkaVirtualActorIngress

Shows how Kafka messages can create and communicate with virtual actors via
Proto.Actor's Kafka integration. Each message from Kafka triggers a virtual
actor, allowing Kafka topics to drive actor workflows.
