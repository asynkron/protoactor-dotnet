# Chat

Demonstrates a simple chat room built with Proto.Remote. A central server spawns
a chat actor that receives messages from connected clients and broadcasts them to
all participants. It showcases remote messaging, client connections and
broadcasting across process boundaries.
