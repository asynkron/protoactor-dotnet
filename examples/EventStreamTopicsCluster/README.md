# EventStreamTopicsCluster

Extends event stream topics to a Proto.Cluster, allowing topic-based publish and
subscribe across nodes. Actors on different machines exchange events through the
cluster's distributed event stream.
