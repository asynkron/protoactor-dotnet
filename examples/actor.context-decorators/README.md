# ContextDecorators

Illustrates how to extend actor behavior by applying custom context decorators.
Decorators wrap the actor's receive pipeline enabling cross-cutting concerns such
as logging or tracing.
