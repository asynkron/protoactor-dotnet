# Activity Log

- Task: Move Mermaid sequence diagram into remoting Endpoints README
- When: $(date -u)
- Environment: workspace-write filesystem, restricted network; no external services started.

## What I did
- Inserted a new section "Sequence: Cross‑Node Messaging Flow" into `src/Proto.Remote/Endpoints/README.md` containing the Mermaid `sequenceDiagram` that explains cross-node messaging via Proto.Remote.
- Removed the previously added `docs/remote-actors-sequence.md` to avoid duplication and make the Endpoints README the single source of truth for this diagram.

## Why this was needed
- Co-locates the communication flow documentation with the remoting endpoints implementation for better discoverability and maintenance.
- Keeps documentation DRY and aligned with the folder’s existing architectural overview and class diagram.

## Notes
- No code or tests were modified; documentation only.
- No .NET exceptions occurred; `logs/exceptions.md` unchanged.

